package convert;

import types.MediaType;

import java.awt.image.BufferedImage;

public class ImageConverter implements Converter<BufferedImage> {
    @Override
    public BufferedImage convert(BufferedImage media, MediaType to_type) {
        return null;
    }
}
